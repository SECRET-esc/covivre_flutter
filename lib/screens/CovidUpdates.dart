import 'package:flutter/material.dart';

class CovidUpdates extends StatelessWidget {
  const CovidUpdates({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      // child: child,
    );
  }
}
